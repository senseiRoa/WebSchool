@model IEnumerable<WebSchool.Models.InstanceOfCourseViewModel>

@{
    ViewBag.Title = "Courses";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- DataTables -->
<link rel="stylesheet" href="~/Content/datatables.net-bs/css/dataTables.bootstrap.min.css">





<div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Clases programadas</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                <table class="table table-bordered table-hover" ">
                    <tr>
                        <th>
                            Curso
                        </th>

                        <th>
                            Fecha de programación
                        </th>
                        <th>
                            Hora Inicial
                        </th>
                        <th>
                            Hora Final
                        </th>

                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.CourseName)
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.StartTime)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.FinalTime)
                            </td>

                            <td>
                                @*@Html.ActionLink("Edit", "Edit", new { id=item.InstanceOfCourseID }) |
                                    @Html.ActionLink("Details", "Details", new { id=item.InstanceOfCourseID }) |
                                    @Html.ActionLink("Delete", "Delete", new { id=item.InstanceOfCourseID })*@
                            </td>
                        </tr>
                    }

                </table>
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    </div>
    <div class="col-md-3"></div>
    <!-- /.col -->
</div>
<!-- /.row -->




@section Scripts {



    <!-- DataTables -->
    <script src="~/Content/datatables.net/js/jquery.dataTables.min.js"></script>
    <script src="~/Content/datatables.net-bs/js/dataTables.bootstrap.min.js"></script>

    <script>
        $(function () {
            //Initialize Select2 Elements
            $('table').DataTable();
        })</script>
}